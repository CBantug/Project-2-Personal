import java.util.*;

/**
 * Method to compare two instances of Movie objects by their title fields. 
 * <P>
 * Algorithm: <br>
 * 1. Compare Movie by their title fields' value
 * 2. Return an int depending upon the outcome of the comparison
 * <P>
 * 
 * @param	o1	The first Movie object to be compared
 * @param	o2	The second Movie object to be compared
 *
 * @return			(0, -1, or 1) depending on the outcome of the comparison
 */
public class IDComparatorTitleM implements Comparator<Movie> {

	@Override
	public int compare(Movie o1, Movie o2) {
		// TODO Write Movie title comparison method
		return 0;
	}

}
